#include <stdio.h>
// M0_A_CONST â€” fixed output, no input, no branch, no loop
// Must be computed inside Malbolge Unshackled VM, not host.
int main() {
    // ASCII only â€” HeLL backend limited
    putchar('O');
    putchar('K');
    putchar('\n');
    return 0;
}

